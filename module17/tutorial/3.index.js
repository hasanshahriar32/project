var numbers = [12, 34, 345, 56];

//get element by index
var second = numbers [1];
console.log(second);

// find index of an element

var position = numbers.indexOf(345);
console.log(position);

var lengths = numbers.length;
console.log(lengths);