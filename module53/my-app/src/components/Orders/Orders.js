import React from 'react';
import Header from '../Header/Header';

const Orders = () => {
    return (
        <div>
            <Header></Header>
            <h1>Orders</h1>
            <h2>Order now to see items</h2>
        </div>
    );
};

export default Orders;